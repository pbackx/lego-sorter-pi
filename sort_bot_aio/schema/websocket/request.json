{
    "$id": "request.json",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "WebSocketRequest",
    "type": "object",
    "properties": {
        "streamCamera": {
            "type": "boolean",
            "description": "Stream the camera to the client or not"
        },
        "bucket": {
            "type": "integer",
            "description": "Move the given bucket under the belt"
        },
        "clearBelt": {
            "type": "boolean",
            "description": "Request to clear the camera belt"
        },
        "makeReferenceImage": {
            "type": "boolean",
            "description": "Make a new reference image, should be done when the belt is empty and is used to detect bricks in front of the camera."
        },
        "nextBrick": {
            "type": "boolean",
            "description": "Move the next brick in front of the camera"
        },
        "stop": {
            "type": "boolean",
            "description": "Stop the machine"
        },
        "start": {
            "type": "boolean",
            "description": "Start the machine"
        },
        "predictOne": {
            "type": "boolean",
            "description": "Move one brick in front of the camera and run the prediction"
        }
    }
}