import { z } from "zod"

export default z.object({ "streamCamera": z.boolean().describe("Stream the camera to the client or not").optional(), "bucket": z.number().int().describe("Move the given bucket under the belt").optional(), "clearBelt": z.boolean().describe("Request to clear the camera belt").optional(), "makeReferenceImage": z.boolean().describe("Make a new reference image, should be done when the belt is empty and is used to detect bricks in front of the camera.").optional(), "nextBrick": z.boolean().describe("Move the next brick in front of the camera").optional(), "stop": z.boolean().describe("Stop the machine").optional(), "start": z.boolean().describe("Start the machine").optional(), "predictOne": z.boolean().describe("Move one brick in front of the camera and run the prediction").optional(), "bucketMapping": z.object({ "default": z.number().int().describe("Default bucket number for unrecognized bricks"), "mapping": z.record(z.array(z.string())).describe("Mapping of bucket numbers to brick types") }).describe("Mapping of bucket numbers to brick types that should go in that bucket").optional() })
